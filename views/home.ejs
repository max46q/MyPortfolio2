<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Portfolio - Back-end Developer</title>
    <style>
      body {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        flex-direction: column;
        font-family: Arial, sans-serif;
      }
      form {
        width: 350px;
        padding: 20px;
        background-color: #f4f4f4;
        border-radius: 5px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }

      label {
        display: block;
        margin-top: 10px;
        font-size: 14px;
      }

      input[type="text"],
      input[type="email"],
      input[type="number"],
      input[type="url"] {
        width: 100%;
        padding: 8px;
        border: 1px solid #ccc;
        border-radius: 4px;
        box-sizing: border-box;
        margin-top: 5px;
      }

      input[type="submit"] {
        width: 100%;
        padding: 10px;
        background-color: #5c3d8a;
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        margin-top: 15px;
      }

      .error {
        color: red;
        margin-top: 10px;
      }

      /* Add styles for the "Open All" button */
      .open-all-btn {
        background-color: #4caf50;
        color: white;
        padding: 10px 20px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        margin-top: 10px;
      }
    </style>
  </head>
  <body>
    <form action="/add" method="post">
      <label for="name">Ім'я:</label>
      <input type="text" id="name" name="name" required />
      <label for="email">Email:</label>
      <input type="email" id="email" name="email" required />

      <label for="age">Вік:</label>
      <input type="number" id="age" name="age" min="1" max="120" required />

      <label for="image">Вставте URL зображення:</label>
      <input type="url" id="image" name="image"  />
      <input type="submit" value="Відправити" />
    </form>

    <!-- Add the "Open All" button -->
    <a href="/all" class="open-all-btn">відкрити всі</a>

    <main>
      <% if (error) { %>
      <h1 class="error"><%= error %></h1>
      <% } %>
    </main>
  </body>
</html>
